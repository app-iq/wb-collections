import { CollectionProvider } from '../Collection/CollectionProvider';

export function Example() {
    return (
        <CollectionProvider>
            <h1>testing</h1>
        </CollectionProvider>
    );
}
